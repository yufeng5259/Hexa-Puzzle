# Supply these as Gradle properties through an untracked local file or CI.
# Real ads are temporarily disabled by default; set true only after filling both IDs.
IAA_ADS_ENABLED=false
# When ads are enabled, release validation rejects missing, malformed or demo values.
IAA_ADMOB_APP_ID=
IAA_REWARDED_AD_UNIT_ID=
